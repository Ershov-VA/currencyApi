"use strict";
exports.__esModule = true;
var express = require('express');
var router = express.Router();
// import { CurrencyController } from '../controllers';
// router.get('/', [auth, viewer], (req:Request, res: Response) => {
//     CurrencyController().getTodayCurrency(req, res)
// })
// router.delete('/:id', [auth, admin], (req: Request, res: Response) => {
//     CurrencyController().remove(req, res)
// })
exports["default"] = router;
